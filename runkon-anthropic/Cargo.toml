[package]
name = "runkon-anthropic"
version = "0.2.0-alpha"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Anthropic API client and Claude agent executor for runkon-flow. Supports both direct API calls and headless Claude CLI subprocesses."
readme = "README.md"
keywords = ["anthropic", "claude", "llm", "agent", "runkon"]
categories = ["api-bindings", "development-tools"]

[features]
test-utils = ["runkon-flow/test-utils"]

[dependencies]
runkon-flow = { path = "../runkon-flow", version = "0.2.0-alpha" }
runkon-flow-executors = { path = "../runkon-flow-executors", version = "0.2.0-alpha" }
runkon-runtimes = { path = "../runkon-runtimes", version = "0.2.0-alpha" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
tracing = "0.1"

[dev-dependencies]
runkon-flow = { path = "../runkon-flow", version = "0.2.0-alpha", features = ["test-utils"] }
tempfile = "3"

[[example]]
name = "standalone_flow"
required-features = ["test-utils"]
